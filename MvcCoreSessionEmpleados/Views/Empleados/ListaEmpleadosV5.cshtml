@model List<Empleado>

<div class="container">
    <h2 class="text-info">Lista de Empleados V5</h2>

    @if (Model != null)
    {
        <div class="container">
            <table class="table table-success table-bordered">
                <thead>
                    <tr>
                        <th>Apellido</th>
                        <th>Salario</th>
                        <th>Oficio</th>
                        <th>ID Departamento</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (Empleado empleado in Model)
                    {
                        <tr>
                            <td>@empleado.Apellido</td>
                            <td>@empleado.Salario.ToString("n0") €</td>
                            <td>@empleado.Oficio</td>
                            <td>@empleado.IdDepartamento</td>
                            <td>
                                <a class="btn btn-danger" asp-action="ListaEmpleadosV5" asp-route-idempleado="@empleado.IdEmpleado">Eliminar</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="container my-4 py-3 text-center bg-danger-subtle text-danger border border-1 border-danger rounded-3">
            <h5>No hay empleados almacenados en Session</h5>
        </div>
    }

    <a asp-action="SessionEmpleadosV5">Volver a la Lista</a>
</div>